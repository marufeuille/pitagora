# ピタゴラスイッチ ゲーム改善メモ

## 優先順位

### 1. サウンド強化
- [x] 衝突音（ボールが床・壁・パーツに当たった音）→ `playBallHit(speed)`
- [x] ドミノが倒れる音 → `playDominoTip()`
- [x] シーソーが傾く音 → `playSeesawCreak()`（角速度監視）
- [x] ボールがゴールに入ったときの音 → `playClear()` 既存
- [x] BGM（ループ、エディット中/プレイ中で切り替え） → `startBGM / switchBGM`

### 2. 複数レベル＋進行管理
- [x] レベルデータ形式の設計 → `src/types/LevelTypes.ts`
- [x] レベルセレクト画面 → `#level-modal-overlay`
- [x] 複数のプリセットレベルを追加（5レベル、難易度順） → `src/levels/levelData.ts`
- [x] クリア後に次のレベルへ進む導線 → 「次のレベル →」ボタン
- [x] 進捗の保存（どこまでクリアしたか） → `LevelManager` + localStorage

### 3. パーツ配置制限
- [x] 各レベルに「使用可能パーツ・個数」の制約を設定 → `constraints` フィールド
- [x] UI に残り配置数を表示 → `.constraint-badge`
- [x] 制限に違反したときのフィードバック → ボタン無効化 + 赤色表示

### 4. 新パーツ追加
- [x] バネ（ボールを弾く） → `src/parts/Spring.ts`（衝突時に上方向インパルス）
- [ ] ファン / 風（軽いオブジェクトを吹き飛ばす） → 複雑なため未実装
- [x] ベル（当たると音がなる演出パーツ） → `src/parts/Bell.ts`
- [ ] 滑車・ロープ（連動する動き） → 複雑なため未実装
- [ ] 跳ね板（バウンスパッド） → バネで代替

### 5. スター評価
- [x] 評価基準の設計（使用パーツ数 ≤ parParts で3星）
- [x] クリア画面に ⭐ × 1〜3 を表示
- [x] レベルセレクトに獲得スターを反映

---

## 現状の既知バグ・改善候補（随時追記）

- [ ] ズームアウト時のセンタリング：ユーザー端末での動作確認
- [ ] 自動化ブラウザでの物理シミュレーション速度低下（タブ非アクティブのスロットリング）
- [ ] ゴール旗の「ピ」フォント：日本語フォントの読み込み確認
